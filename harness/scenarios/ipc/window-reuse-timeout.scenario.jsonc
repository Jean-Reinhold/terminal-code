{
  "$schema": "../../schemas/scenario-v1.schema.json",
  "schema_version": 1,
  "id": "ipc.window-reuse.timeout",
  "title": "Existing window timeout is preserved",
  "contracts": ["C05"],
  "risk": "critical",
  "targets": { "mode": "single", "target": "rust-core" },
  "requires": {
    "os": ["macos", "linux"],
    "capabilities": ["process", "unix-socket"],
    "timeout_ms": 5000
  },
  "sandbox": { "environment": {}, "network": "not-required" },
  "steps": [
    {
      "id": "window",
      "kind": "unix_socket.server",
      "environment": "TODE_IPC",
      "reply": { "ok": true },
      "delay_ms": 100,
      "max_request_bytes": 4096,
      "timeout_ms": 1000
    },
    {
      "id": "client",
      "kind": "process.exec",
      "program": { "target": "target-probe" },
      "args": ["ipc-open-timeout", "{\"files\":[],\"folders\":[],\"add\":false}", "20"],
      "stdin": "closed",
      "capture": ["stdout", "stderr", "exit"],
      "timeout_ms": 1000
    }
  ],
  "observations": [
    { "id": "result", "kind": "process.result", "from": "client" },
    { "id": "wire", "kind": "unix_socket.transcript", "from": "window" }
  ],
  "assertions": [
    {
      "kind": "exact.snapshot",
      "observation": "result",
      "snapshot": "harness/baselines/ipc/window-reuse-timeout.process.json"
    }
  ]
}
